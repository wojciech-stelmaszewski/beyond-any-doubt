# Beyond Any Doubt — Task List

- **Session timestamp:** 2026-09-04_11-49
- **Source plan:** `docs/plan-2026-09-04_11-49.md`
- **Status:** Awaiting approval. Do not start Phase 1 until Section 8 of the plan is signed off.

Mark items `[x]` as they are completed.

---

## Phase 0 — Approval Gate

- [x] 0.1 Review the "Ink & Proof" palette against the rendered preview (`docs/palette-preview.html`).
- [x] 0.2 Verify the palette in a Brettel-based CVD simulator (covers tritanopia, which the
      Viénot simulation does not model reliably).
- [x] 0.3 Decide: KaTeX first, or MathJax 4 immediately.
- [x] 0.4 Decide: native Sätteri pipeline, or `@astrojs/markdown-remark` compatibility layer.
- [x] 0.5 Decide: Source Serif 4 + KaTeX, or STIX Two Text + STIX Two Math.
- [x] 0.6 Confirm Astro over Quarto.
- [x] 0.7 Record the decisions at the end of `docs/plan.md`.

## Phase 1 — Project Skeleton

- [x] 1.1 Initialise an Astro 7 project with `output: 'static'` and TypeScript in strict mode.
- [x] 1.2 Pin exact dependency versions; commit the lockfile.
- [x] 1.3 Add `.editorconfig`, Prettier and ESLint; enable format-on-save.
- [x] 1.4 Set `site: 'https://beyond-any-doubt.org'` and leave `base` as `/` (apex domain).
- [x] 1.5 Define content collections (`posts`) with a Zod schema: `title`, `description`, `date`,
      `updated`, `tags`, `draft`, `math`.
- [ ] 1.6 Add a `README.md` describing local development and the deployment flow.

## Phase 2 — Maths Pipeline (highest technical risk — do this before anything else)

- [x] 2.1 **Spike:** configure Sätteri with `features: { math: true }` and `satteriKatex()` in
      `mdastPlugins` (**not** `hastPlugins`) and confirm both inline and display maths render.
- [x] 2.2 Write a maths torture-test post: `align`, `cases`, `matrix`, `\operatorname`, custom
      macros, nested fractions, large operators, `\substack`.
- [x] 2.3 Confirm zero client-side JavaScript is shipped for maths.
- [x] 2.4 Load KaTeX CSS only on pages with `math: true` in frontmatter.
- [x] 2.5 Define global KaTeX macros in one place (`src/config/katex-macros.ts`).
- [x] 2.6 Establish an equation numbering and referencing convention (`\tag{}` plus stable anchors).
- [x] 2.7 Put all maths rendering behind a single component boundary so the renderer can be swapped.
- [x] 2.8 Document the fallback route (`@astrojs/markdown-remark` + `remark-math` + `rehype-katex`)
      in `README.md`, including how to switch.
- [x] 2.9 **Gate:** if 2.1–2.3 fail, switch to the fallback before continuing.

## Phase 3 — Design System

- [x] 3.1 Create `src/styles/tokens.css` with all light-theme custom properties from plan §3.1–3.2.
- [x] 3.2 Add the dark-theme overrides under `[data-theme="dark"]` from plan §3.3–3.4.
- [ ] 3.3 Author tint/shade ramps in OKLCH with sRGB hex fallbacks.
- [x] 3.4 Self-host and subset the fonts as WOFF2; preload the body and heading faces.
- [ ] 3.5 Build the typographic scale: body ~19–20px, line height 1.65, measure ~68 characters.
- [ ] 3.6 Implement the theme toggle: default to `prefers-color-scheme`, persist a manual override,
      and apply it before first paint to avoid a flash.
- [x] 3.7 Implement a `--accent` focus ring (3px, 2px offset) visible on every surface.
- [ ] 3.8 Build semantic callout components: Definition, Theorem, Lemma, Proof, Conjecture,
      Counterexample. Each carries an icon and a label — never colour alone.
- [ ] 3.9 Enforce a maximum of four colour-coded semantic categories visible at once.
- [ ] 3.10 Add a `/styleguide` page rendering every token, callout and chart series.
- [ ] 3.11 Automate contrast checking in CI so no token can regress below its documented ratio.

## Phase 4 — Charts

- [ ] 4.1 Add Observable Plot and `linkedom`; build a build-time SVG rendering utility.
- [ ] 4.2 Make chart colours read the CSS custom properties so themes switch without re-rendering.
- [ ] 4.3 Implement the five-series light and dark palettes from plan §3.5.
- [ ] 4.4 Enforce redundant encoding above five series: dash pattern, marker symbol, direct labels.
- [ ] 4.5 Configure `viridis`/`cividis` for sequential data and a balanced diverging map for signed
      data; forbid rainbow maps in review.
- [ ] 4.6 Add a `<Figure>` component with a caption, a stable id and a permalink anchor.
- [ ] 4.7 Add one interactive chart island hydrated with `client:visible` as a reference example.
- [ ] 4.8 Verify charts remain readable in greyscale print.

## Phase 5 — Content Layer and Pages

- [ ] 5.1 Base layout: skip link, landmark regions, correct heading hierarchy.
- [ ] 5.2 Post layout: title, dates, reading time, tags, table of contents.
- [ ] 5.3 Home page with a chronological post list.
- [ ] 5.4 Post archive and per-tag pages.
- [ ] 5.5 About page.
- [ ] 5.6 404 page.
- [ ] 5.7 RSS feed and `sitemap.xml`.
- [ ] 5.8 Open Graph metadata plus generated OG images.
- [ ] 5.9 Print stylesheet: light theme forced, links expanded to URLs, figures kept unbroken.
- [ ] 5.10 Write one full reference post exercising maths, charts, code and every callout type.

## Phase 6 — Deployment

- [ ] 6.1 Add `.github/workflows/deploy.yml` building on push to `main` and deploying via
      `actions/deploy-pages`.
- [ ] 6.2 Set Repository Settings → Pages → Source: **GitHub Actions**.
- [ ] 6.3 Add `public/CNAME` containing `beyond-any-doubt.org`.
- [ ] 6.4 Configure DNS: apex `A`/`AAAA` (or `ALIAS`) records plus a `www` `CNAME`.
- [ ] 6.5 Wait for certificate issuance, then enable **Enforce HTTPS**.
- [ ] 6.6 Add a build-time link checker and fail the build on broken internal links.
- [ ] 6.7 Add a Lighthouse CI budget: performance and accessibility ≥ 95.

## Phase 7 — Optional, Post-Launch (YAGNI gate — only if actually needed)

- [ ] 7.1 Pagefind static search.
- [ ] 7.2 Giscus comments backed by GitHub Discussions.
- [ ] 7.3 Privacy-respecting analytics.
- [ ] 7.4 Reader-facing maths accessibility: MathJax 4 explorer, if reader feedback asks for it.

---

## Definition of Done

A phase is complete when every checkbox is ticked, the build passes, the Lighthouse budget holds,
and the work is committed to the branch.
