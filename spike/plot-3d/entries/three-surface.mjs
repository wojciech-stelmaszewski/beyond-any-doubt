// Same, plus what a shaded surface needs.
import {
  AmbientLight, BufferGeometry, Color, DirectionalLight, DoubleSide, Float32BufferAttribute,
  Line, LineBasicMaterial, LineSegments, Mesh, MeshLambertMaterial, PerspectiveCamera,
  Points, PointsMaterial, Scene, Vector3, WebGLRenderer,
} from "three";
import { OrbitControls } from "three/examples/jsm/controls/OrbitControls.js";
export { AmbientLight, BufferGeometry, Color, DirectionalLight, DoubleSide, Float32BufferAttribute,
  Line, LineBasicMaterial, LineSegments, Mesh, MeshLambertMaterial, OrbitControls,
  PerspectiveCamera, Points, PointsMaterial, Scene, Vector3, WebGLRenderer };
