// What a hand-rolled scene actually needs: camera, orbit, lines, points, text-free axes.
import {
  BufferGeometry, Color, Float32BufferAttribute, Line, LineBasicMaterial,
  LineSegments, PerspectiveCamera, Points, PointsMaterial, Scene, Vector3, WebGLRenderer,
} from "three";
import { OrbitControls } from "three/examples/jsm/controls/OrbitControls.js";
export { BufferGeometry, Color, Float32BufferAttribute, Line, LineBasicMaterial, LineSegments,
  OrbitControls, PerspectiveCamera, Points, PointsMaterial, Scene, Vector3, WebGLRenderer };
